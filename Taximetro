<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taxímetro - Andalucía RP</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap');
        body {
            font-family: 'Roboto', sans-serif;
            background-image: url('https://tse3.mm.bing.net/th/id/OIP.33vNFqZH7DzOXOVUahgWVgHaEK?w=264&h=180&c=7&r=0&o=5&pid=1.7'); /* Reemplaza con la URL de tu imagen de fondo */
            background-size: cover;
            background-position: center;
            color: #333;
            text-align: center;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            position: relative;
        }
        h1 {
            color: #007BFF;
            font-size: 48px;
            font-family: 'Roboto', sans-serif;
            text-shadow: 0 0 10px rgba(0, 123, 255, 0.5);
        }
        button {
            padding: 15px 30px;
            font-size: 18px;
            margin: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        #startButton {
            background-color: #4CAF50;
            color: white;
        }
        #startButton:hover {
            background-color: #45a049;
        }
        #pauseButton {
            background-color: #ff9800;
            color: white;
        }
        #pauseButton:hover {
            background-color: #fb8c00;
        }
        #stopButton {
            background-color: #f44336;
            color: white;
        }
        #stopButton:hover {
            background-color: #e53935;
        }
        .display {
            font-size: 24px;
            margin: 20px 0;
        }
        .description {
            font-size: 18px;
            margin-top: 20px;
            color: #555;
        }
        .taximeter {
            display: inline-block;
            padding: 10px;
            background-color: #555;
            color: #ff0000;
            border-radius: 5px;
            font-family: 'Courier New', Courier, monospace;
        }
        .developer {
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 14px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="developer">Desarrollado por Nautfe</div>
        <h1>Taxímetro Andalucía RP</h1>
        <div>
            <button id="startButton">Iniciar</button>
            <button id="pauseButton">Pausar</button>
            <button id="stopButton">Parar</button>
        </div>
        <div class="display">
            <div class="taximeter">
                <h2>Tiempo: <span id="time">0</span> segundos</h2>
                <h2>Costo: €<span id="cost">0.00</span></h2>
            </div>
        </div>
        <div class="description">
            Esta página web está hecha para el uso de la empresa de taxis de Andalucía RP.
        </div>
    </div>

    <script>
        let startTime;
        let elapsedTime = 0;
        let timer;
        const costPerSecond = 0.75;

        document.getElementById('startButton').addEventListener('click', () => {
            startTime = Date.now() - elapsedTime * 1000;
            timer = setInterval(updateTimeAndCost, 1000);
        });

        document.getElementById('pauseButton').addEventListener('click', () => {
            clearInterval(timer);
            elapsedTime = Math.floor((Date.now() - startTime) / 1000);
        });

        document.getElementById('stopButton').addEventListener('click', () => {
            clearInterval(timer);
            elapsedTime = 0;
            document.getElementById('time').textContent = elapsedTime;
            document.getElementById('cost').textContent = (elapsedTime * costPerSecond).toFixed(2);
        });

        function updateTimeAndCost() {
            elapsedTime = Math.floor((Date.now() - startTime) / 1000);
            document.getElementById('time').textContent = elapsedTime;
            document.getElementById('cost').textContent = (elapsedTime * costPerSecond).toFixed(2);
        }
    </script>
</body>
</html>
